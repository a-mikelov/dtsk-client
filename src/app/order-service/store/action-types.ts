export enum ActionTypes {
  ORDER_SERVICE = `[Order service] Send order`,
  ORDER_SERVICE_SUCCESS = `[Order service] Send order success`,
  ORDER_SERVICE_FAILURE = `[Order service] Send order failure`,

  ORDER_SERVICE_WEBHOOK = `[Order service] Send order webhook`,
  ORDER_SERVICE_WEBHOOK_SUCCESS = `[Order service] Send order webhook success`,
  ORDER_SERVICE_WEBHOOK_FAILURE = `[Order service] Send order webhook failure`,
}
